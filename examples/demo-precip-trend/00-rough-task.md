# readme.md (as dropped into the working directory)

need to check if station precip is trending up or down over the last 20 years for the
region in `data/stations/`. colleague mentioned some stations look wetter in recent years
but nobody's actually checked. want a plot and a quick writeup, maybe a couple of slides
for the Friday group meeting.

data is monthly totals, csv per station, ~40 stations. some have gaps.
